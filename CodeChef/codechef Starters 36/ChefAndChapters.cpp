#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;

    cin >> t;

    while (t--)
    {
        long int x, y, z;
        cin >> x >> y >> z;
        cout << (x*y*z) <<endl;
    }
    return 0;
}